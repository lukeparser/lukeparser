name: Test build

on:
  push:
    branches:
      - '*'
      - '!master'
      - '!main'

jobs:
  CIBW:
    uses: lukeparser/lukeparser/.github/workflows/cibuildwheel.yml@dev-cibw
    with:
      fail-fast: false
      deploy: false
